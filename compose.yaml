services:
  sigmo:
    image: damonto/sigmo:latest
    container_name: sigmo
    restart: unless-stopped
    network_mode: host
    privileged: true
    volumes:
      - ./data:/root/.local/share/sigmo
      - /dev:/dev
      - /run/udev:/run/udev:ro
      - /sys:/sys:ro
